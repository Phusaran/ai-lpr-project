# 🚗 ระบบตรวจจับและอ่านป้ายทะเบียนรถยนต์อัตโนมัติ (AI LPR System)

ระบบเว็บแอปพลิเคชันสำหรับตรวจจับและอ่านป้ายทะเบียนรถยนต์ไทยและรถจักรยานยนต์ที่มีความแม่นยำสูง โดยใช้สถาปัตยกรรม **Hybrid YOLOv12** (ทำงานร่วมกัน 2 โมเดล: ตรวจจับป้าย + อ่านตัวอักษร) พัฒนาด้วย **Python Flask** (Backend) และ **React** (Frontend)

## 🚀 ฟีเจอร์หลัก (Features)

* **Hybrid AI Architecture:** ใช้โมเดล YOLOv12 สองตัวทำงานร่วมกันเพื่อความแม่นยำสูงสุด
    1.  `plate_detector`: ค้นหาตำแหน่งกรอบป้ายทะเบียน
    2.  `char_detector`: อ่านตัวอักษรและจังหวัด (รองรับ 124 คลาส)
* **รองรับภาษาไทยสมบูรณ์:** อ่านพยัญชนะไทย ตัวเลข และชื่อจังหวัดได้ถูกต้อง พร้อมระบบวาดผลลัพธ์ภาษาไทยด้วย Pillow
* **Smart Voting System (ระบบโหวต):** มีอัลกอริทึม Buffer เก็บค่าและโหวตผลลัพธ์ เพื่อให้ค่าที่แสดงนิ่งและไม่กระพริบ
* **Ghost Box Prevention:** ระบบลบกรอบค้างอัตโนมัติเมื่อรถขับออกจากเฟรม
* **แก้ปัญหาป้ายมอเตอร์ไซค์:** มีระบบขยายขอบภาพ (Padding) อัตโนมัติ เพื่อแก้ปัญหาตัวอักษรริมขอบถูกตัดขาด
* **ใช้งานง่าย:** หน้าเว็บรองรับการ Drag & Drop วิดีโอ, ควบคุม Play/Pause และดู Log ย้อนหลังได้

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

* **AI/ML:** Ultralytics YOLOv12, PyTorch
* **Backend:** Python, Flask, OpenCV, Pillow, NumPy
* **Frontend:** React.js (Vite)

## 📂 โครงสร้างโปรเจค (Project Structure)

lpr-web-app/

├── backend/           # ส่วนประมวลผล AI และ API (Python Flask)

│   ├── models/        # ที่เก็บไฟล์โมเดล YOLOv12 (.pt)

│   ├── app.py         # ไฟล์หลักสำหรับรัน Server

│   └── ...

└── frontend/          # ส่วนหน้าเว็บ (React)
    ├── src/           # โค้ด React Components
    └── ...
    
⚙️ การติดตั้ง (Installation)
1. ดาวน์โหลดโปรเจค (Clone)
Bash

git clone (https://github.com/Phusaran/ai-lpr-project)
cd lpr-web-app

2. ติดตั้งฝั่ง Backend (Python)
เข้าไปที่โฟลเดอร์ backend และตั้งค่าสภาพแวดล้อมจำลอง (Virtual Environment)

cd backend

# สร้าง Virtual Environment
python -m venv venv

# เปิดใช้งาน (Activate)
# สำหรับ Windows:
.\venv\Scripts\activate
# สำหรับ Mac/Linux:
source venv/bin/activate

# ติดตั้ง Library ที่ต้องใช้
pip install flask flask-cors ultralytics opencv-python pillow numpy
❗ สำคัญ: ดาวน์โหลดโมเดล AI
เนื่องจากไฟล์โมเดลมีขนาดใหญ่ ไม่สามารถอัปโหลดขึ้น GitHub ได้โดยตรง กรุณาดาวน์โหลดไฟล์โมเดลทั้ง 2 ตัวจากลิงก์ด้านล่าง:

👉 ดาวน์โหลดไฟล์โมเดล (.pt) ที่นี่ https://drive.google.com/drive/u/1/folders/1Ozej5EwIGru30ZKWCUV5Xnk3NWNtuVO5

เมื่อดาวน์โหลดเสร็จแล้ว ให้นำไฟล์ plate_detector.pt และ char_detector.pt ไปวางไว้ในโฟลเดอร์ backend/models/

3. ติดตั้งฝั่ง Frontend (React)
เปิด Terminal ใหม่ และเข้าไปที่โฟลเดอร์ frontend

cd frontend

# ติดตั้ง Node modules
npm install
▶️ วิธีใช้งาน (How to Run)
ต้องเปิด 2 Terminal เพื่อรันระบบคู่กัน:

Terminal 1: รัน Backend

# เข้าไปที่ backend และ activate venv ก่อน
cd backend
.\venv\Scripts\activate

# รันเซิร์ฟเวอร์
python app.py
# (Server จะรันที่: [http://127.0.0.1:5001](http://127.0.0.1:5001))
Terminal 2: รัน Frontend

# เข้าไปที่ frontend
cd frontend

# รันหน้าเว็บ
npm run dev
# (Web App จะรันที่: http://localhost:5173)
เปิด Browser และเข้าใช้งานที่ http://localhost:5173
